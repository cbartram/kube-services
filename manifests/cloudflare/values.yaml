# Default values for cloudflared.

replicaCount: 2

image:
  repository: cloudflare/cloudflared
  pullPolicy: IfNotPresent
  tag: "latest"

resources:
  requests:
    cpu: 10m
    memory: 256Mi
  limits:
    memory: 512Mi